interface:
  display_name: "Git Commit Assistant"
  short_description: "Safely split Git commits and tag milestones"
  default_prompt: "Use $git-commit to safety-check Git-known changes, create well-scoped commits, or tag milestones on the latest commit."

policy:
  allow_implicit_invocation: true
