interface:
  display_name: "English Commit Splitter"
  short_description: "Split English Git commits by functional flow"
  default_prompt: "Use $commit_english to inspect the current workspace, split changes into multiple safe commits, never mix unrelated topics in one commit, and write concise English conventional commit messages."

policy:
  allow_implicit_invocation: true
