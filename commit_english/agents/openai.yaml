interface:
  display_name: "English Commit Splitter"
  short_description: "Split Git changes into clear English commits"
  default_prompt: "Use $commit_english to split Git-known changes by functional flow and add concrete bullet bodies when one commit contains multiple coordinated subchanges."

policy:
  allow_implicit_invocation: true
